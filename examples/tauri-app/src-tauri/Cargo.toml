[package]
name = "tauri-plugin-system-components-demo"
version = "0.1.0"
description = "Standalone demo of tauri-plugin-system-components."
edition = "2021"
rust-version = "1.77.2"
license = "MIT"
publish = false

[lib]
name = "tauri_plugin_system_components_demo_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2.5", default-features = false, features = [] }

[dependencies]
tauri = { version = "2.10", features = ["macos-private-api"] }
tauri-plugin-system-components = { path = "../../../" }
